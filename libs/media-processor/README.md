# media-processor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build media-processor` to build the library.

## Running unit tests

Run `nx test media-processor` to execute the unit tests via [Jest](https://jestjs.io).
